
branches:
  only:
  - master

language: node_js

script: npm run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: build
  keep-history: true
  on:
    branch: master
